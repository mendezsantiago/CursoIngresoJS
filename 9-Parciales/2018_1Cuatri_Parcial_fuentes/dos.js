function mostrar()
{
  var nombre = document.getElementById("elNombre").value;                       //Entrada de datos
  var localidad = document.getElementById("laLocalidad").value;

  alert ("Usted es " + nombre + " y vive en la localidad de " + localidad);     //Salida de datos
  
}
